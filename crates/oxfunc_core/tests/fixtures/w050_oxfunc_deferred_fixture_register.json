[
  {
    "scenario_id": "A04",
    "seam_family": "prepared_argument",
    "defer_reason": "Requires a reference-bind profile; grid-reference resolution moved to OxCalc strict-excel-grid provider after core became grid-agnostic. Covered by OxCalc grid_reference_machine + OxFunc oxfml_minimal_reference_functions."
  },
  {
    "scenario_id": "A05",
    "seam_family": "prepared_argument",
    "defer_reason": "Requires a reference-bind profile; grid-reference resolution moved to OxCalc strict-excel-grid provider after core became grid-agnostic. Covered by OxCalc grid_reference_machine + OxFunc oxfml_minimal_reference_functions."
  },
  {
    "scenario_id": "A06",
    "seam_family": "prepared_argument",
    "defer_reason": "Requires a reference-bind profile; grid-reference resolution moved to OxCalc strict-excel-grid provider after core became grid-agnostic. Covered by OxCalc grid_reference_machine + OxFunc oxfml_minimal_reference_functions."
  },
  {
    "scenario_id": "A07",
    "seam_family": "prepared_argument",
    "defer_reason": "Requires a reference-bind profile; grid-reference resolution moved to OxCalc strict-excel-grid provider after core became grid-agnostic. Covered by OxCalc grid_reference_machine + OxFunc oxfml_minimal_reference_functions."
  },
  {
    "scenario_id": "A08",
    "seam_family": "prepared_argument",
    "defer_reason": "Requires a reference-bind profile; grid-reference resolution moved to OxCalc strict-excel-grid provider after core became grid-agnostic. Covered by OxCalc grid_reference_machine + OxFunc oxfml_minimal_reference_functions."
  },
  {
    "scenario_id": "B01",
    "seam_family": "implicit_intersection",
    "defer_reason": "Parses after the @-precedence fix (b1dab42) but still needs a reference-bind profile to resolve, and @ (implicit intersection) over a reference is not yet correctly evaluated (e.g. @A1 -> #VALUE! even via the real provider). Pending @-evaluation follow-up (Workstream B / OxCalc)."
  },
  {
    "scenario_id": "B02",
    "seam_family": "implicit_intersection",
    "defer_reason": "Parses after the @-precedence fix (b1dab42) but still needs a reference-bind profile to resolve, and @ (implicit intersection) over a reference is not yet correctly evaluated (e.g. @A1 -> #VALUE! even via the real provider). Pending @-evaluation follow-up (Workstream B / OxCalc)."
  },
  {
    "scenario_id": "B04",
    "seam_family": "implicit_intersection",
    "defer_reason": "Parses after the @-precedence fix (b1dab42) but still needs a reference-bind profile to resolve, and @ (implicit intersection) over a reference is not yet correctly evaluated (e.g. @A1 -> #VALUE! even via the real provider). Pending @-evaluation follow-up (Workstream B / OxCalc)."
  },
  {
    "scenario_id": "B06",
    "seam_family": "implicit_intersection",
    "defer_reason": "Parses after the @-precedence fix (b1dab42) but still needs a reference-bind profile to resolve, and @ (implicit intersection) over a reference is not yet correctly evaluated (e.g. @A1 -> #VALUE! even via the real provider). Pending @-evaluation follow-up (Workstream B / OxCalc)."
  },
  {
    "scenario_id": "B07",
    "seam_family": "implicit_intersection",
    "defer_reason": "Parses after the @-precedence fix (b1dab42) but still needs a reference-bind profile to resolve, and @ (implicit intersection) over a reference is not yet correctly evaluated (e.g. @A1 -> #VALUE! even via the real provider). Pending @-evaluation follow-up (Workstream B / OxCalc)."
  },
  {
    "scenario_id": "F01",
    "seam_family": "cross_seam",
    "defer_reason": "Requires a reference-bind profile; grid-reference resolution moved to OxCalc strict-excel-grid provider after core became grid-agnostic. Covered by OxCalc grid_reference_machine + OxFunc oxfml_minimal_reference_functions."
  },
  {
    "scenario_id": "F02",
    "seam_family": "cross_seam",
    "defer_reason": "Requires a reference-bind profile; grid-reference resolution moved to OxCalc strict-excel-grid provider after core became grid-agnostic. Covered by OxCalc grid_reference_machine + OxFunc oxfml_minimal_reference_functions."
  },
  {
    "scenario_id": "F03",
    "seam_family": "cross_seam",
    "defer_reason": "Requires a reference-bind profile; grid-reference resolution moved to OxCalc strict-excel-grid provider after core became grid-agnostic. Covered by OxCalc grid_reference_machine + OxFunc oxfml_minimal_reference_functions."
  },
  {
    "scenario_id": "F04",
    "seam_family": "cross_seam",
    "defer_reason": "Parses after the @-precedence fix (b1dab42) but still needs a reference-bind profile to resolve, and @ (implicit intersection) over a reference is not yet correctly evaluated (e.g. @A1 -> #VALUE! even via the real provider). Pending @-evaluation follow-up (Workstream B / OxCalc)."
  },
  {
    "scenario_id": "F05",
    "seam_family": "cross_seam",
    "defer_reason": "Requires a reference-bind profile; grid-reference resolution moved to OxCalc strict-excel-grid provider after core became grid-agnostic. Covered by OxCalc grid_reference_machine + OxFunc oxfml_minimal_reference_functions."
  }
]
