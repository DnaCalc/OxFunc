# Candidate Closure Sweep — ATANH and TBILLYIELD — 2026-07-10

Status: `tbillyield_closed`; `atanh_open`.

## Scope and environment

This is the expanded sign-off requested after the bounded 24-row
reconnaissance pass. It tests only the two credible closure candidates.

- Excel: `16.0` build `20131`
- workbook Compatibility Version: `2`
- host axis: x86-64
- input plumbing: exact binary64 through worksheet `Range.Value2` cell refs
- comparison: typed outcome and exact result bits, no tolerance
- runner: `smart-fuzzer/tools/Run-CandidateClosureSweeps.ps1`
- compact rollup: `docs/function-lane/CANDIDATE_CLOSURE_ROLLUP_20260710.csv`

## ATANH — remains open

The proposed x87-LN graph
`0.5*LN((1+x)/(1-x))` was deliberately tested beyond the two central witnesses.
It was not globally valid:

| Implementation | Cases | Exact | Mismatch | Result |
|---|---:|---:|---:|---|
| x87-LN candidate | 368 | 297 | 71 | rejected and reverted |
| restored odd-symmetric platform kernel | 368 | 235 | 133 | current baseline; row remains open |

The candidate catastrophically collapses tiny inputs when the ratio rounds to
`1`, and also regresses near-boundary values. For example Excel preserves
`ATANH(f64::from_bits(1)) = f64::from_bits(1)`, while the ratio/LN candidate
returns zero. The existing stable central witnesses remain canonical:

- `ATANH(0.1)`: current local `0x3fb9af93cd234413`, Excel
  `0x3fb9af93cd234415` (`2` ULP).
- `ATANH(0.2)`: current local `0x3fc9f323ecbf984c`, Excel
  `0x3fc9f323ecbf9849` (`3` ULP).

The calculation map is updated to search a piecewise legacy kernel rather than
a single worksheet-LN identity.

## TBILLYIELD — discrepancy row closed

The pre-repair expanded matrix reproduced the previously unretained residual:

- `2,156` cases total;
- `1,848` exact;
- `308` mismatches, every one exactly `1` ULP.

The minimized witness is:

`TBILLYIELD(36526,36533,98.45)`

| Path | Bits |
|---|---|
| prior OxFunc `((100-pr)/pr*360)/days` | `0x3fe9e90181246c31` |
| Excel | `0x3fe9e90181246c32` |
| repaired `((100-pr)/pr)*(360/days)` | `0x3fe9e90181246c32` |

After changing only that association, the identical live-Excel matrix is
`2,156/2,156` exact with no structural or numeric mismatches.

Matrix axes:

- 14 settlement dates spanning 2000–2026, leap/EOM and ordinary dates;
- 14 maturity durations: `1,2,7,28,30,31,60,90,91,180,182,183,364,365` days;
- 11 prices: `50,80,90,95,98,98.45,99,99.5,99.9,99.99,100`.

The repair is locally explained, exercised, and non-regressing on the declared
current-baseline matrix. The stale G6 TBILLYIELD row is removed.

## TBILLYIELD pre-closure checklist

This checklist closes the current-baseline discrepancy row, not alternate
Excel application versions/channels or locale phases.

| # | Result | Evidence |
|---|---|---|
| 1 | Yes | Existing contract and function surface are unchanged. |
| 2 | Yes | The Lean family model covers metadata/admission and a TBILLYIELD seed; binary64 association remains in the documented Rust/executable substrate. |
| 3 | Yes | Focused regression and full `oxfunc_core --lib` validation pass. |
| 4 | Yes | The 2,156-row deterministic replay artifact is generated by the repository runner. |
| 5 | Yes | Environment, matrix axes, minimized witness, and exact bits are recorded here. |
| 6 | Yes | Excel build and workbook Compatibility Version are explicit. |
| 7 | Yes | Empirical Excel association wins over mathematical equivalence. |
| 8 | Yes | Direct COM/cell-ref verification does not rely on the XLL seam. |
| 9 | Yes | No FEC/F3E or evaluator-facing contract changed. |
| 10 | Yes | No known TBILLYIELD gap remains in the declared current-baseline matrix. |
| 11 | Yes | Closure language is restricted to this discrepancy row/current baseline. |
| 12 | Yes | Catalog, worklist, calculation map, bug stream, and W108 records are reconciled. |
| 13 | Yes | No new blocker or cross-repo dependency was created. |

Completion-claim self-audit: scope re-read `pass`; gate criteria re-read `pass`;
silent scope reduction check `pass`; scaffolding/test/contract/Lean/handoff
pattern check `pass`.

## Status axes

- `scope_completeness`: `scope_complete` for this two-lane closure audit.
- `target_completeness`: `target_partial` because ATANH remains open.
- `integration_completeness`: `integrated` for the TBILLYIELD repair and evidence;
  ATANH has no promoted candidate.
- `open_lanes`: ATANH piecewise-kernel search; alternate Excel CPU/version axes;
  wider financial-family exactness rows unrelated to TBILLYIELD.
