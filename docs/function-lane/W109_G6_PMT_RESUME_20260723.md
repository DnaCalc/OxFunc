# W109 G6-01 PMT/annuity — RESUME (2026-07-23)

Compaction handoff. Supersedes the confused middle of the session; read this first, then the
dated 2026-07-23 sections of `W109_G6_BOND_SCHEDULE_IDENTIFICATION_20260720.md`.

## STATE: two production fixes LANDED, one primitive wall remains

**LANDED (all committed, 1513 lib tests pass, 0 regressions):**
- `1eb9011` — **PMT op-graph corrected** in `crates/oxfunc_core/src/functions/financial_time_value_family.rs::pmt`.
  Production had used a *refuted* reciprocal-multiply combine + portable `exp_portable`/`excel_expm1`.
  Now: quotient-first SSE2 combine `pmt = RN(RN(RN(num/em)/tf)·r)`, `num=pv+fv·v`, `v=1+em`,
  `em=excel_expm1_internal(-n·excel_log1p(r))` (x87 Kahan), `exp` via `excel_exp` (fFEXP), **log1p kept
  correctly-rounded** (`excel_log1p`). Measured on live-Excel oracles: **heldout 46%→56%, combsweep
  62%→100%, po2 61%→87%, genrate 47%→73%, fvty 36%→43%, fv1sweep 36%→50%.**
- `5082589` — **IPMT epsilon-band bug fixed** (line ~802): `abs()<EPSILON→0.0` collapsed tiny AND negative
  rates to zero + dropped sign. Now exact `r==0` (live-Excel: `IPMT(1e-13,1,360,2e5)=−2e-8` flows through
  main path; `IPMT(0,..)=+0.0`). Test `ipmt_tiny_rate_flows_through_main_path`. PPMT=`pmt−ipmt` inherits.

## RETRACTED this session (DO NOT re-chase — both fell to confound-free oracles)
1. **"Excel PMT log1p is non-CR / faithful"** — FALSE. log1p IS correctly-rounded (= x87 FYL2XP1,
   bit-identical to CR software on `0/1350` confound-free rows). Proven via the `|tau|≥1` oracle where
   `em=u−1` is exact so only log1p matters. `std ln_1p`'s +30 heldout was pure expm1-wall COMPENSATION
   (`std−CR=+32` in `|tau|<1` confounded, `−2` in `|tau|≥1` clean). Kahan-companion log1p refuted at general
   rates too. Production `excel_log1p` (CR) is CORRECT — do not swap.
2. **"em is not a function of tau_double / extended argument"** — FALSE. The collision "variation" (em
   varies at fixed CR-tau0) was the expm1's OWN ±1 ULP rounding noise (impurity floor ~33 for EVERY faithful
   candidate; em doesn't sort by exact tau). The configs shared tau0 only under mpmath-CR log1p; that was a
   design artifact, not extended precision.

## THE ONE REMAINING WALL: expm1 |tau|<1 non-CR Kahan double-rounding
- Confound-free (log1p=CR at exact-1+r). `em=(u−1)·t/ln(u)` all-double ceilings at **163/234**; the x87
  double-rounded numerator `em=RN53(RN64((u−1)·t)/ln(u))` is **165/234** (+2, already in the sense that
  `excel_expm1_internal` is close; landing the RN64 numerator is a possible +2 micro-win).
- EXHAUSTIVELY refuted: all associations/additive/reciprocal (≤163), extended-precision stagings (≤152),
  F2XM1 (127), reciprocal-F2XM1 (94), PC=53 (151), RZ/chop (112–130), polynomial fdlibm(129)/Cephes(84),
  x87 spill schedules (9216-config search, 165 max). `|tau|≥1` branch `em=u−1` is 100% exact.
- Character: faithful ~0.5 ULP **toward-zero** bias, **denominator-`lnu`-attributable** (69/71 misses fixable
  by `nextafter(lnu,±1)`; 57/71 == `nextafter(dbl_Kahan,0)`), signed error drifts monotone **+0.585→−0.324
  ULP across |tau| 1e-6..1** (minimax-error fingerprint). It is OUTSIDE the `{tau,u,ln u}` op-graph family
  → must be **fit as g(tau)** (Gauss-Newton on the exact-tau subset n=2^j), not raced. Or accept as the
  tracked Excel imprecision. IPMT/PPMT/CUMIPMT/CUMPRINC INHERIT it — closing PMT closes all 5.

## FACTS (solid, reusable)
- Combine = **SSE2 quotient-first, ·rate LAST, tf a DIVIDE** (7 combine forms tested; only this reproduces
  the oracle; x87/reciprocal/annuity-factor forms reproduce ZERO configs). `v=1+em` (not `exp(tau)`) is
  slightly better on fvty.
- `excel_exp`=fFEXP (proven 234/234 double args); `excel_log1p`=CR; `excel_expm1_internal`=x87 Kahan.
- Model-free em: at `r=2^-k`, `·r` is exact → `pmt=2^-k·RN(pv/em)`, 128 consecutive pv pin em. General r:
  128 pv still pin ~90% unique.
- Heldout residual splits by |tau|: `|tau|<1` = expm1 wall; `|tau|≥1` = a separate ~15% combine/fv/v residual
  (broad −30..+17 ULP misses — the fv≠0/ty=1 assembly, next-cleanest target).

## TOOLING (smart-fuzzer/tools/calc_graph_racer/src/bin/, cargo run --release --bin NAME from that dir)
race_pmt_prod_vs_fix (prod vs corrected op-graph), race_log1p_id + verify_log1p_split (log1p=CR proof),
race_collide_search / doubt_combine (combine=SSE2), race_spill_exhaustive / race_ext_em / race_expm1_small
(expm1 wall), export_em. Oracle runner: smart-fuzzer/tools/Run-W109BulkBatch.ps1 -Batch in.json -Out out.json.

## DATA (smart-fuzzer/work/w109/G6-solvers/, gitignored)
answers-pmt-{heldout,combsweep,po2,po2n,genrate,collide,fvty,denselog1p,em}.json; collide-meta.json;
expm1_intermediates.csv (k,n,tau_bits,u_bits,lnu_bits,em_pinned); em_consolidated.csv; answers-{ln-exact,
expondist,pow-*,ipmt-eps,m-*}.json; WORKFLOW_RESULTS.md + WORKFLOW2_RESULTS.md (8-agent digests); COORDINATOR_NOTES.md.

## NEXT (clean, isolated targets)
1. **expm1 wall as g(tau) fit** — Gauss-Newton a minimax/rational on the exact-tau (n=2^j) subset driven by
   the signed `delta(tau)=em−CR`, evaluated in double per-op; OR land the RN64-numerator +2 and catalog the rest.
2. **fv≠0/ty=1 combine residual** (the `|tau|≥1` ~15%, broad misses) — the num=pv+fv·v assembly at large fv.
3. IPMT/PPMT exact balance recurrence (per-period vs closed FV); CUM* fold order. All inherit the PMT fix.
