# W109 open-lane sweep (2026-07-12)

One pass over the non-solver open rows looking for reasonable-effort
closures. Live oracle build 20131; ~200 fresh probes.

## CLOSED: FORECAST / FORECAST.LINEAR (G3-03 partial) — promoted

Excel's FORECAST is NOT the LINEST pipeline. Identified kernel
(65/65 bit-exact over discovery + adversarial: 1e12 common offsets, n=2,
near-constant x, constant y, n=12 irregular, subnormal magnitudes):

    mx = (Σx)/n ; my = (Σy)/n           forward sums, plain double
    num = Σ (x-mx)(y-my) ; den = Σ (x-mx)²   one fused loop
    b = num/den ; a = my - b·mx ; result = a + b·x   (intercept form)

OxFunc rerouted `forecast_pair_kernel` off `trend_kernel` onto this
kernel with pinned witnesses; full lib suite green (1497).

Bonus validations: SLOPE 4/4, INTERCEPT 4/4 — the existing
paired_stats_common implementations already ARE this kernel (no change).

TREND: 7/12 under the simple kernel with every publication variant ->
TREND really is on the least-squares machinery; stays open with
LINEST/LOGEST (normal-equations staging identification).

## SCOPED: ATANH (G4-02) + ACOTH (G4-03)

107+57 fresh rows. Excel ATANH on |x| < ~0.5 shows an EQUIOSCILLATING
error vs true (+6.3..-3.4 ULP, damping toward the band edge) -> custom
Microsoft odd rational, log-form ~CR above. UCRT/Cephes/fdlibm/x87-pair
candidates all ruled out (ledger updated). ACOTH inherits through its
reciprocal. Same coefficient-recovery class as GAMMALN's (0,11) core —
queue both for the Phase-5b integer-relation fitter.

**Supersession (2026-08-09):** both July inferences above are historical and
retracted. ATANH's independent exact cubic/stored-ratio graph landed in
`a03a75f`. ACOTH's separate answer-blind campaign identified a native-ratio /
stored direct-inverse-series graph at exact threshold
`0x400d92b14ec204f3`; its candidate-frozen held-out is `66552/66552` and
production replay is `268769/268769`. See
`W109_ATANH_IDENTIFICATION_20260712.md` and
`W109_ACOTH_IDENTIFICATION_20260809.md`.

## BLOCKED (recorded, no effort spent)

- MINVERSE (G5-01): array-valued result; CellRefBatch reads back a single
  value cell — needs a wrapper-formula lane (e.g. INDEX(MINVERSE(...),i,j))
  or matrix readback in the oracle plumbing.
- CHISQ.TEST / F.TEST (G3-05/06): publish through the incomplete-beta/
  chi-square tail substrate (G3-01) — bottom-up substrate first.
- CONVERT (G4-05): **superseded 2026-08-09** — typed text/exact-bit plumbing
  landed, the exact three-store graph passed a frozen `10418/10418`
  publication gate and `34189/34189` production replay, and G4-05 is signed
  off. See `W109_CONVERT_IDENTIFICATION_20260809.md`.

## Cache state

~2,000 oracle rows added this weekend across IRR/RATE/NPV/FV/PMT/PV/
ATANH/ACOTH/FORECAST/TREND/SLOPE/INTERCEPT.

## Unblock round (same day): MINVERSE and CHISQ.TEST/F.TEST

### MINVERSE (G5-01) — unblocked, 47/51

The blocker dissolved on inspection: CellRefBatch already wraps matrix
calls in `INDEX(call, r, c)` with a `result_index` seam, and OracleCache
already keys on it. Run-W109ProbeBatch just dropped the field — patched
(plus 2-D matrix args: nested arrays -> rows x cols row-major). 51
element probes over six 2x2 and three 3x3 matrices:
- adjugate closed form: ruled out EVEN FOR 2x2 (Excel eliminates);
- Gauss-Jordan (any pivoting): ruled out (23/51 max);
- **LU (Doolittle, partial pivot, division form) + per-column unit-vector
  solves: 47/51**; the 4 residuals are +1 ULP each, isolated to 3x3
  factorization staging (Crout ordering / x87 / FMA next), invariant
  under all forward/backward solve variants.

### CHISQ.TEST (G3-05) — statistic half unblocked by decomposition

Proven on live bits: CHISQ.TEST(o,e) == CHIDIST(S, df) EXACTLY, S a
stored double. Probing CHIDIST at candidate statistic bit-patterns makes
the tail cancel, so the statistic staging is identifiable today: on the
first witness S = row-major sum + 1 ULP -> a small accumulation race
(more shapes) pins it. The CHIDIST tail itself stays gated on the G3-01
gamma/beta substrate. F.TEST unblocks identically via FDIST.

## Enrichment + generalization (same day)

Banked 108 fresh MINVERSE 3x3 element rows and 77 ATANH band rows as
held-out sets.

- MINVERSE: the Doolittle-PP-division algorithm GENERALIZES to
  **150/159** on the combined corpus; Crout (36), FMA (111), and
  reciprocal-multiply (130) are decisively ruled out. Algorithm
  identified; 9 residual <=1-ULP misses on ill-conditioned matrices are
  the factorization-inner-loop accumulation detail (final staging axis).
- ATANH passthrough threshold pinned: largest x with ATANH(x)==x is
  1.0985e-8; first deviation at 1.42805e-8 (matches x³/3 < ½ ulp).
