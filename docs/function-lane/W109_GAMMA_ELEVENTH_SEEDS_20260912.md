# GAMMA elevenths in (0,1) (2026-09-12)

Live Excel 16.0 build 20326 / CV2, Range.Value2 inject.

`GAMMA(k/11)` for k=1..10 are private seeds. `GAMMA(1/11)` is 2 ULP from
the generic path. Recurrence not claimed.
