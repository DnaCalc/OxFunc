# T.DIST df=2 PDF = POWER(2+x*x, -1.5) (2026-09-12)

Live Excel 16.0 build 20326 / CV2, Range.Value2 inject.

`T.DIST(x,2,FALSE)=POWER(2+x*x,-1.5)` 15/15 on a signed grid including
`|x|=4,8,10`. The same score holds for `1/POWER(2+x*x,1.5)`,
`1/((2+x*x)*SQRT(2+x*x))`, and `1/EXP(1.5*LN(2+x*x))` in Excel.

IEEE `1/((2+x*x)*sqrt)` is 4/15. Spilled `1/excel_exp(1.5*excel_log)` is
10/15. `excel_pow_chain(2+x*x, -1.5)` is 15/15.

`0.5/POWER(1+x*x/2,1.5)` is 0/15 (wrong scale).
