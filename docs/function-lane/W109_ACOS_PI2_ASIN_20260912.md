# ACOS = PI()/2 − ASIN (2026-09-12)

Live Excel 16.0 build 20326 / CV2, Range.Value2 inject.

`ACOS(x)=PI()/2-ASIN(x)` 14/14 discovery plus 20/20 holdout (34/34),
including x=0.75 where `ATAN2(x,SQRT(1-x*x))` is 1 ULP.
`PI()-ASIN` is the wrong complement (0/9). ATAN2 forms are 11/14 max 1 ULP.

libm `acos` is 1 ULP off `ACOS(0.5)`. Production now uses the worksheet
`PI()/2-ASIN` identity.
