# T.DIST(x,1,FALSE) = 1/(PI()*(1+x*x)) (2026-09-12)

Live Excel 16.0 build 20326 / CV2, Range.Value2 inject.

Cauchy PDF: `T.DIST(x,1,FALSE)=1/(PI()*(1+x*x))` 20/20 including
x=0, ±0.5, ±1, ±2, ±4, ±10, 100. Split `1/PI()/(1+x*x)` is 12/20.
`1/(PI()+PI()*x*x)` is 18/20.

The Cauchy CDF `0.5+ATAN(x)/PI()` is 6/9 max 4 ULP and is not landed.
T.INV.2T df=1 remains the TAN closed form from the same campaign day.
