# Local Cache

Derived indexes and generated helper artifacts go here.

This directory is ignored by default except for this README. Cache contents must
be reproducible from tracked inputs before any result depending on them is
promoted.
